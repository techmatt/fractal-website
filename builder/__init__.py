"""The page generator for this site.

Run on a workstation, never in the deploy path: what it writes is committed HTML, and
GitHub Pages serves the commit rather than a build. A broken builder cannot take the
site down; it can only fail to produce the next diff. See `README.md`.
"""
